---
- name: Restart postgres
  systemd: 
    name: "{{postgres_systemd}}"
    state: restarted

- name: Restart sshd
  systemd:
    name: sshd
    state: restarted

- name: Restart app server
  systemd:
    name: "{{app_server}}" 
    state: restarted

- name: Upgrade DB schema
  shell: "source /home/{{oval_user}}/.bashrc ; . {{venv_dir}}/bin/activate &&  {{app_dir}}/manage.py db upgrade"
  args:
    chdir: "{{app_dir}}"
  become: true
  become_user: "{{oval_user}}"

- name: Restart nginx
  systemd:
    name: nginx
    state: restarted
